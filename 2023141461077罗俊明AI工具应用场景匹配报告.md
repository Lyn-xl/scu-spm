# AI工具应用场景匹配报告

> 软件过程与管理 · 作业第一部分
> 2026年6月12日—7月9日

## 起因

这个作业要求我们在传统的软件过程里加入AI能力。说实话一开始我还挺懵的——AI辅助写代码我平时确实在用（Copilot还是挺香的），但要说系统性地把AI融进整个软件过程，之前还真没认真想过。

所以我做的第一件事就是花了几天时间把市面上主流的AI软件工程工具都大致过了一遍。看了一圈发现，2024到2026年这块发展确实太快了，去年还在讨论"AI能不能写代码"，今年就已经在讨论"AI什么时候能独立完成一个模块"了。下面是我调研下来觉得比较靠谱的一些发现。

## 调研过程

我主要看了这几个方面的资料：一是学术论文，ICSE和ASE这两年的会议论文集里AI相关的文章占比已经超过三分之一了；二是行业报告，Gartner和Stack Overflow的年度调查都很有参考价值；三是工具本身的文档和社区反馈，毕竟好不好用只有用了才知道。

调研过程中有个感受挺深的——很多工具宣传得很厉害，但实际用起来跟宣传的差距还是不小的。比如有些号称能"自动生成完整测试用例"的工具，生成的用例质量其实很一般，边界条件基本覆盖不到。但也有些工具确实超出了我的预期，尤其是Claude Code在多文件代码生成和审查方面的表现。

## 五个值得关注的应用方向

### 1. 需求阶段的AI辅助

需求这块其实是我觉得最有意思但也最难做好的。传统的需求调研基本就是开会、访谈、写文档，很多时间花在了整理会议纪要和反复确认理解上。

我觉得AI在这块的切入点可以有两个。一个是需求采集阶段，用对话式的AI引导利益相关者把需求讲清楚——很多时候用户自己也不知道自己要什么，AI可以追问和引导。Notion AI现在已经有类似的功能了，虽然还比较初级。另一个是需求文档的质量检查，比如发现前后矛盾的描述、遗漏的边界条件，这个用LLM来做歧义检测效果出奇地好。我在做上一个课程项目的时候就遇到过这样的情况——需求文档里写着"用户登录失败3次锁定账号"，但后面又写了"支持密码重置，不限次数"，当时人工评审谁都没注意到这个矛盾，现在想想如果用AI扫一遍应该一下就能发现。

工具方面，Notion AI和Claude在这块都可以用，成本也不高。

### 2. 设计阶段——让AI画UML

说实话我觉得UML图画起来真的很烦。不是说它没用，而是手动画图、维护同步实在太花时间了。项目做到后期代码改了十几版，当初画的类图早就对不上了。

现在用Claude Code加上Mermaid，你把需求描述丢进去，它能自动生成类图、时序图、部署图的初稿。虽然不是100%准确（大概能有七八成的样子），但改起来比自己从零画快多了。而且因为是用文本描述生成的，后续需求变了只要改描述重新生成就行，不用在Visio里拖来拖去。

另外CodeScene这个工具挺有意思的，它不画图，而是分析你的代码仓库推荐设计模式。比如它发现你的代码里有一堆if-else判断不同类型，就会建议用策略模式重构。虽然有时候建议比较死板，但作为参考还是不错的。

### 3. 编码阶段——最好用的部分

这大概是AI目前最成熟的应用场景了。我自己这半年写代码基本离不开Copilot和Claude Code，估计70%以上的样板代码都是AI生成的。

具体来说有几个使用方式。一是常规的代码补全，Copilot X现在能根据整个项目的上下文来补，不像早期版本只能看当前文件。二是用自然语言描述一个功能，AI直接生成完整的代码文件——这个功能对CRUD类的业务模块特别好用，但对于复杂算法逻辑就差点意思。三是代码审查，我最近试了用Claude Code做PR Review，它确实能发现一些人工容易忽略的问题，比如N+1查询、事务边界这些。当然也有误报，大概10%左右。

不过有个坑要说一下：AI生成的代码不是拿来就能用的。它经常会自作主张地引入一些奇怪的依赖或者不符合项目规范的写法。所以我现在的习惯是AI生成完我必看一遍，有些地方要改，有些要补注释。总之就是"用AI的产量，用自己的判断"。

### 4. 测试阶段——意外的惊喜

测试这块我之前对AI没抱太大期望，但实际用下来反而是惊喜最大的。

我用了Testin XAgent和Claude来生成测试用例，效果比想象中好。特别是边界条件的覆盖——AI会很机械地把各种边界都给你列出来，null值、空字符串、超长输入、并发冲突等等。这些东西人工写测试的时候经常会偷懒漏掉，但AI不会，它没有"差不多得了"的概念。

效率方面，原来写一个模块的单元测试可能要两三个小时，现在大概半小时就能搞定初稿，然后再花半小时审查补充。主要省下来的时间是那些重复性高的测试数据构造和断言编写。

不过集成测试和端到端测试的AI生成质量就差一些了。因为这些测试需要理解业务流程和用户操作习惯，AI目前在这方面还是不太行。

### 5. 运维阶段

运维这块我自己经验不多，主要参考了一些公司的实践案例。Datadog的AI异常检测和PagerDuty的告警聚合是比较成熟的应用，核心思路就是用AI替代人工从告警堆里找真正严重的问题。

有意思的是，AI在故障分析报告生成方面也很有用。原来是运维工程师花一两个小时写事故报告，现在让AI根据日志和监控数据自动生成初稿，工程师只需要审核补充，据说能把写报告的时间从2小时压到20分钟。

## 我的优先级判断

如果让我给一个小团队推荐AI工具引入的优先级，我会这样排：

第一优先级肯定是**编码和代码审查**。这是ROI最高的，工具成熟、效果好、学习成本低，基本装上就能用。GitHub Copilot X加Claude Code的组合目前是最优解。

第二是**测试用例生成**。这个的收益也很明显，但需要一点配置和调优。Testin XAgent或直接用Claude的API都行。

第三是**设计阶段的UML生成**。这个对架构师的帮助比较大，但对普通开发者来说可能感知不强。

需求和运维阶段的AI应用我个人觉得还处在比较早期的阶段，中小团队可以先观望，等工具更成熟了再说。

## 总结

花了差不多一周调研下来，最大的感受是：AI在软件过程中不是"替换人"而是"让人做更有价值的事"。把重复性的、模式化的工作交给AI，人把精力放在真正需要创造力和判断力的地方——这个方向是对的。

但也不能神话AI。它生成的代码还是要审，它给的测试用例还是要补，它画的UML图还是要改。说到底AI就是个效率工具，跟当年的IDE、版本控制一样，用好了能省很多时间，但软件质量最终还是要靠人来把关。

---

**参考资料**（完整列表见综合作业报告）：
- 中国信通院《智能化软件测试技术与应用要求》2024
- Gtest《全流程自动化测试技术白皮书》2025
- GitHub Copilot Workspace官方文档 2025
- AI4SE行业现状调查报告 2024
- 陈翔等《AI原驱动自动化测试方式研究》软件学报 2025
