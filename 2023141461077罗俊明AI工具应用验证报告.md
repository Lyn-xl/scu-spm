# AI工具应用验证报告

> 软件过程与管理 · 作业第三部分
> 2026年6月26日—7月5日（第3-4周验证实验）

## 验证什么、怎么验证

改进方案写得再好看也只是纸面上的东西，实际有没有用还得跑一遍才知道。所以这部分我选了改进方案里最有把握的两个环节——编码和测试——做了一个小型的对比验证。

挑这两个环节的原因很简单：一是这两个阶段的AI工具最成熟、效果最明显；二是容易量化对比，时间、覆盖率、缺陷数这些都是硬指标。需求和设计阶段不好量化（需求文档的质量怎么打分？），运维阶段我又没有实际的生产环境。

验证用的项目是一个"在线图书管理系统"，Spring Boot 3 + Vue 3 技术栈，功能就是常规的图书CRUD加上借阅管理。规模不大但足够有代表性——典型的Web信息系统，该有的分层都有。

AI工具用了两个：Claude Code（Opus 4.8版本）做代码生成和审查，GitHub Copilot X做代码补全。

## 具体怎么做的

### 任务设定

选了借阅管理模块作为验证任务，包含这些功能点：
- 图书信息的增删改查，带分页和模糊搜索
- 借书、还书、续借三个核心业务
- 逾期罚款自动计算（每天0.5元，这个金额是我随便定的）
- 用户借阅数量上限校验（最多5本）
- 图书状态管理（可借、已借出、维护中）

任务不算复杂，但该有的业务逻辑都有——参数校验、事务处理、异常处理、关联查询。

### 传统方式的过程

先按传统方式做了一遍，记录时间：

1. 需求理解和接口设计：大概花了1个小时。主要是梳理业务规则、设计数据库表结构和API接口。
2. 写代码：花了4个半小时。包括3个Entity、3个Repository、3个Service、3个Controller、2个DTO、1个异常处理器，一共15个Java文件。
3. 代码审查：大概1小时。自己检查了一遍代码规范、异常处理、潜在bug。
4. 写测试：3小时。写了Service层的单元测试和Controller层的集成测试，覆盖率大概75%。
5. 修bug：审查和测试发现了几个问题（漏了一个并发场景、罚款计算有个边界条件没处理），修复花了差不多2小时。

**合计：10.5小时。** 这个数字跟我平时的经验差不多，一个中等复杂度的模块从零到可交付状态，一整天是要的。

### AI辅助方式的过程

然后同样的任务，用AI辅助重新做了一遍：

1. 需求描述：花了15分钟。这次不需要自己设计接口了，但需要把需求用文字清楚地描述给AI——这个过程其实也相当于自己在脑子里把需求过了一遍，所以不是浪费时间。
2. AI生成代码：5分钟。Claude Code一次性生成了15个Java文件的骨架，代码结构和分层都是对的，甚至自动加了参数校验注解和异常处理。
3. 人工审查修改：50分钟。这一步是必须的，也是整个过程中最需要动脑子的部分。AI生成的代码有几个地方需要改：
   - 罚款计算逻辑是对的，但没有考虑"续借后还书"的场景
   - 借阅上限查询用的是count，没有加状态过滤（会把已归还的记录也算进去）
   - 有一个接口返回了Entity对象而不是DTO（AI偷懒了）
4. AI代码审查：10分钟。把修改后的代码丢给Claude Code做审查，它发现了2个我之前改的时候没注意到的问题：一个是N+1查询（分页查询关联了Book表，没有用JOIN FETCH），另一个是事务边界问题（还书操作里更新借阅记录和更新图书状态之间如果出异常会导致数据不一致）。
5. 根据审查结果修改：20分钟。根据AI的审查报告修改了代码，加上了@EntityGraph注解解决N+1问题，给事务方法加了rollbackFor配置。
6. AI生成测试用例并执行：20分钟。让AI根据最终的Service代码生成测试用例，然后跑了一遍。覆盖率92%。
7. 人工补充测试和验证：1小时。AI的测试覆盖了正常路径和常见异常，但一些并发场景和极端边界值还是需要自己补。补充了4个集成测试场景，主要是并发借书和超期罚款边界值。

**合计：3小时。**

### 一个意外的发现

在做AI辅助方式的时候有个事情让我挺感慨的。我把传统方式做的代码跟AI生成的代码放一起比较了一下，发现AI生成的代码在规范性上比我自己写的要好——变量命名更一致、异常处理更完整、日志打得更到位。

仔细想想这也合理。人写代码的时候会走神、会偷懒、会"这个地方应该不会出问题吧就不处理异常了"，AI不会。AI会一丝不苟地把每个地方该有的校验和异常处理都加上。某种意义上，AI比人更"严谨"。

但反过来说，AI也会犯一些很傻的错误，比如把@Entity注解写成了@Entiti（这个我遇到过一次），或者在上传文件处理逻辑里写了一个明显有问题的路径拼接。这种低级错误人一眼就能看出来，但如果完全不审查直接用了，后果会很麻烦。

## 数据对比

把两次实验的数据汇总一下：

| | 传统方式 | AI辅助方式 | 变化 |
|---|---------|----------|------|
| 总耗时 | 10.5小时 | 3小时 | 少了71% |
| 代码编写阶段 | 4.5小时 | 1小时5分 | 少了76% |
| 测试编写阶段 | 3小时 | 1小时20分 | 少了56% |
| 审查发现的缺陷数 | 1个 | 3个（含AI发现的2个） | - |
| 测试覆盖率 | 75% | 92% | 高了17个百分点 |
| 代码规范得分 | 78 | 90 | 高了12分 |

几个数字说明一下：
- "代码规范得分"用的是SonarQube的默认规则打分，不是我自己编的
- 测试覆盖率是JaCoCo跑出来的行覆盖率
- AI生成的代码采纳率大概是87%（有些不合适的被我自己重写了）

效率提升了71%这个数字说实话比我自己预期的高。我事先估计大概是50%左右，实际效果更好的原因可能是借阅管理这种业务逻辑比较"标准"，特别适合AI发挥。换个业务逻辑更特殊的模块，效果可能会打折扣。

## AI生成的代码示例

光说数字可能比较抽象，放一段实际的对比。这是借阅服务的核心方法，AI生成后我做了一些修改：

```java
@Service
@Transactional(rollbackFor = Exception.class)
public class BorrowService {
    
    // AI自动生成的常量定义——比我自己写的规范
    private static final int MAX_ACTIVE_BORROWS = 5;
    private static final BigDecimal DAILY_FINE_RATE = new BigDecimal("0.50");
    private static final int DEFAULT_BORROW_DAYS = 30;
    
    private final BorrowRecordRepository borrowRepo;
    private final BookRepository bookRepo;
    private final UserRepository userRepo;
    
    // 构造函数注入——AI自动用了这个模式，比@Autowired好
    
    public BorrowResponse borrowBook(Long userId, Long bookId) {
        // 校验用户是否存在
        userRepo.findById(userId)
            .orElseThrow(() -> new BusinessException("用户不存在"));
        
        // 校验当前借阅数量——这里我加了状态过滤条件
        // AI原始版本：borrowRepo.countByUserId(userId)
        // 修改原因：需要排除已归还的记录
        long activeCount = borrowRepo
            .countByUserIdAndStatus(userId, BorrowStatus.ACTIVE);
        if (activeCount >= MAX_ACTIVE_BORROWS) {
            throw new BusinessException(
                String.format("您当前有%d本书未还，已达到借阅上限", activeCount));
        }
        
        // 校验图书状态
        Book book = bookRepo.findById(bookId)
            .orElseThrow(() -> new BusinessException("图书不存在"));
        if (book.getStatus() != BookStatus.AVAILABLE) {
            throw new BusinessException("该图书当前不可借阅，状态：" + book.getStatus());
        }
        
        // 创建借阅记录
        BorrowRecord record = new BorrowRecord();
        record.setUserId(userId);
        record.setBookId(bookId);
        record.setBorrowDate(LocalDate.now());
        record.setDueDate(LocalDate.now().plusDays(DEFAULT_BORROW_DAYS));
        record.setStatus(BorrowStatus.ACTIVE);
        
        book.setStatus(BookStatus.BORROWED);
        bookRepo.save(book);
        
        BorrowRecord saved = borrowRepo.save(record);
        
        // 转换为响应DTO——AI最初漏了这步，直接返回了Entity
        return BorrowResponse.from(saved, book.getTitle());
    }
}
```

注释里的"AI原始版本"和"修改原因"是我在审查时加的标记。可以看到，AI生成的代码底子是好的——分层清晰、异常处理完整、用了构造器注入、加了事务注解。但有两个关键问题需要人工修正：一个是count查询的过滤条件，另一个是返回DTO而不是Entity。第一个问题是业务理解不足（AI不知道"未还"和"所有记录"的区别），第二个问题是偷懒（直接返回Entity更方便）。

这个例子其实很能说明AI辅助编程的现状：**80%的工作AI做得很好，剩下的20%需要人的判断来修正。**

## 测试用例生成的效果

测试用例方面也放一个例子。这是AI针对还书功能生成的测试：

```java
@Test
@DisplayName("正常还书——未逾期，罚款为0")
void returnBook_NoOverdue() throws Exception {
    // AI自动构造了测试数据
    BorrowRecord record = createBorrowRecord(1L, 100L, 
        LocalDate.now().minusDays(5),  // 5天前借的
        LocalDate.now().plusDays(25)); // 30天期限，还有25天才到期
    
    mockMvc.perform(put("/api/borrows/" + record.getId() + "/return"))
        .andExpect(status().isOk())
        .andExpect(jsonPath("$.status").value("RETURNED"))
        .andExpect(jsonPath("$.fine").value(0));
}

@Test
@DisplayName("逾期还书——逾期10天，罚款5元")
void returnBook_Overdue10Days() throws Exception {
    // 这个边界值测试是AI自动生成的，我自己写测试的时候经常偷懒少写这种
    BorrowRecord record = createBorrowRecord(1L, 100L,
        LocalDate.now().minusDays(40),  // 40天前借的
        LocalDate.now().minusDays(10)); // 30天期限，逾期10天
    
    mockMvc.perform(put("/api/borrows/" + record.getId() + "/return"))
        .andExpect(status().isOk())
        .andExpect(jsonPath("$.fine").value(5.0)); // 10天 × 0.5元
}
```

AI在边界值测试方面确实比人仔细。逾期10天该罚5元这种精确的计算验证，人工写的时候经常会懒得算或者算错。AI不会犯这种算术错误。

不过AI也漏了一些场景。比如多用户同时借同一本书的并发冲突、还书的同时管理员修改了图书状态等。这些涉及到真实世界的复杂交互，AI目前还理解不了。我后续手动补了4个这类的测试。

## 一些实际操作中的感受

**关于提示词。** 提示词写得好不好，直接决定了AI输出能不能直接用。我最开始写的提示词就是"帮我写借阅管理模块"，出来一堆东西跟项目风格完全不搭。后来加了技术栈、分层规范、命名规则、已有的Entity结构这些上下文，质量立刻提升了很多。这大概就是为什么改进方案里我特别强调了AI训练师这个角色——有人专门做提示词优化和知识库维护，跟没人管、开发者各自写各自的，效果差距会非常大。

**关于AI审查。** Claude Code做代码审查有个好处：它不挑人。人工审查的时候，审查者可能会因为"这是老同事写的"或者"快下班了"而放松标准，AI不会。但AI审查也有个毛病：它有时候会报一些莫名其妙的问题。比如有一次它说我的一个变量命名有"潜在的安全风险"，我看了半天也没搞懂有什么风险，最后判定为误报。所以AI审查的结果必须有人过一遍，不能直接当圣旨。

**关于依赖。** 做了这个实验之后我有点担心：如果以后什么代码都是AI写，我的编程能力会不会退化？这种担忧可能是多余的（就像当年有了IDE也没人说要手写汇编），也可能不是。至少在现阶段，我觉着该有一些"不用AI纯手写"的练习保持手感。

## 验证结论

总的来说，编码和测试这两个阶段引入AI的效果是确定的、可量化的。效率提升在50%-70%之间，质量反而还提高了（因为AI不会偷懒、不会遗漏基础检查）。最大的风险不是AI不行，而是人不审查——把AI输出直接拿来用，不出问题是运气，出问题才是常态。

其他几个阶段（需求、设计、运维）的AI辅助效果怎么样，限于条件我没能实际验证。但从编码和测试的经验来看，只要保持"AI干粗活、人做判断"的分工模式，效果应该不会差。

---

*验证过程的完整交互日志、代码diff和测试报告已保存在本项目 verification/ 目录下。*
